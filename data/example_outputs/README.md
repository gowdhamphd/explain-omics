# Example outputs

These are small sample outputs generated by running the pipeline on `data/example_inputs/`.

- `Top500_Triplets.csv` — ranked microbe–pathway–host-module chains.
- `Top300_Chains4.csv` — ranked 4-layer chains if gene lists are available.
