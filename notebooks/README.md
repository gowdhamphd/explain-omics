# Notebooks

- **01_inference_demo.ipynb** — run the pipeline (if needed) and perform partial-input inference queries.

Tip: open the notebook after installing dependencies (`pip install -r requirements.txt`).
